$DISKSPACE stopsession nobutton keep
message Diskspace: $(DS=$(df -B 1M . | tail -n 1 | awk '{ print $4 }');  
  [ $DS -lt 1000 ] && echo "<h1>! Warning !</h1> <h2>Less than <b>${DS}MB</b> 
  of free disk space left!
  </h2> Please make backups and clean up to make room for the next recording!")


#############################################################################

$Idle fallback keep
Pause: repeats=0;

$Resistance
MembraneResistance

$Threshold latencies
ThresholdLatencies

$V-I Curve
VICurve

$f-I Curve
FICurve

$long f-I Curve: duration=2000ms; pause=6000ms; imin=0.001nA; imax=0.100nA; istep=0.002nA;
FICurve: duration=$duration; pause=$pause; repeat=1; imin=$imin; imax=$imax; istep=$istep;

$Find DC
FindThreshold
SetDC:
  dcamplitudesel=relative to threshold;
  dcamplitudedecr=0.0;
  interactive=true;

$Set DC
SetDC: dcamplitudesel=to previous value;

$Set Leak
SetLeak: interactive=true;

$Reset
SetLeak: interactive=false; preset=zero; reversaltorest=false;
SetDC: dcamplitudesel=to absolute value; dcamplitude=0.0; interactive=false;

$XY
VoltageReconstruction